🚨 خطأ في النظام
🔧 حلول خطأ الخادم الداخلي (500)
🚨 التشخيص السريع للأخطاء
1. تحقق من السجلات (Logs)
python
# أضف هذا الكود في ملف app.py لرؤية الأخطاء
import traceback
import logging

logging.basicConfig(level=logging.DEBUG)

@app.errorhandler(500)
def internal_error(error):
    print("🔍 ERROR DETAILS:")
    print(traceback.format_exc())
    return "Internal Server Error", 500
2. التحقق من المتطلبات الأساسية
افتح terminal وتحقق من:

bash
# تحقق من تثبيت المكتبات
python -c "import flask; print('Flask OK')"
python -c "import transformers; print('Transformers OK')"
python -c "import torch; print('PyTorch OK')"
python -c "import sqlite3; print('SQLite OK')"

# تحقق من إصدارات المكتبات
pip list | grep -E "(flask|transformers|torch|sqlalchemy)"
🔧 الإصلاحات الفورية
الإصلاح 1: إعادة تشغيل الخادم
bash
# أوقف الخادم الحالي (Ctrl+C)
# ثم أعد التشغيل:
python main.py
# أو
python app.py
الإصلاح 2: تحديث المكتبات
bash
pip install --upgrade flask transformers torch
الإصلاح 3: التحقق من الملفات المفقودة
python
# تأكد من وجود جميع الملفات الضرورية
required_files = [
    'app.py', 
    'models/intent_classifier.py',
    'models/image_processor.py', 
    'database/database.py'
]

for file in required_files:
    try:
        with open(file, 'r'):
            print(f"✅ {file} موجود")
    except FileNotFoundError:
        print(f"❌ {file} مفقود!")
🛠️ كود بديل سريع للتشغيل
ملف app_fixed.py بديل:
python
from flask import Flask, request, jsonify, render_template
import sqlite3
import os
import logging

# إعداد التسجيل
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger(__name__)

app = Flask(__name__)

# إنشاء قاعدة بيانات بسيطة
def init_db():
    conn = sqlite3.connect('chatbot.db')
    c = conn.cursor()
    c.execute('''
        CREATE TABLE IF NOT EXISTS conversations
        (id INTEGER PRIMARY KEY AUTOINCREMENT,
         user_input TEXT,
         bot_response TEXT,
         timestamp DATETIME DEFAULT CURRENT_TIMESTAMP)
    ''')
    conn.commit()
    conn.close()
    print("✅ قاعدة البيانات جاهزة")

# الصفحة الرئيسية
@app.route('/')
def home():
    return '''
    <!DOCTYPE html>
    <html>
    <head>
        <title>Medical Chatbot</title>
        <meta charset="utf-8">
        <style>
            body { font-family: Arial, sans-serif; margin: 40px; }
            .chat-container { max-width: 800px; margin: 0 auto; }
            .message { padding: 10px; margin: 5px; border-radius: 10px; }
            .user { background: #007bff; color: white; text-align: right; }
            .bot { background: #f1f1f1; color: black; text-align: left; }
        </style>
    </head>
    <body>
        <div class="chat-container">
            <h1>🤖 Medical Chatbot - النسخة المؤقتة</h1>
            <div id="chat"></div>
            <input type="text" id="userInput" placeholder="اكتب رسالتك هنا...">
            <button onclick="sendMessage()">إرسال</button>
        </div>
        
        <script>
            function sendMessage() {
                const input = document.getElementById('userInput');
                const message = input.value;
                
                if (message.trim() === '') return;
                
                // عرض رسالة المستخدم
                const chat = document.getElementById('chat');
                chat.innerHTML += `<div class="message user">👤: ${message}</div>`;
                
                // إرسال للخادم
                fetch('/chat', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ message: message })
                })
                .then(response => response.json())
                .then(data => {
                    chat.innerHTML += `<div class="message bot">🤖: ${data.response}</div>`;
                    input.value = '';
                    chat.scrollTop = chat.scrollHeight;
                })
                .catch(error => {
                    console.error('Error:', error);
                    chat.innerHTML += `<div class="message bot">❌ خطأ في الاتصال</div>`;
                });
            }
            
            // السماح بالإرسال بالزر Enter
            document.getElementById('userInput').addEventListener('keypress', function(e) {
                if (e.key === 'Enter') sendMessage();
            });
        </script>
    </body>
    </html>
    '''

# نقطة نهاية الدردشة
@app.route('/chat', methods=['POST'])
def chat():
    try:
        user_message = request.json.get('message', '')
        logger.info(f"📨 رسالة المستخدم: {user_message}")
        
        # رد بسيط مؤقت
        response = generate_response(user_message)
        
        # حفظ في قاعدة البيانات
        save_conversation(user_message, response)
        
        return jsonify({'response': response})
    
    except Exception as e:
        logger.error(f"❌ خطأ في /chat: {str(e)}")
        return jsonify({'response': 'عذراً، حدث خطأ في النظام. جرب مرة أخرى.'}), 500

def generate_response(message):
    """دالة بسيطة لتوليد الردود"""
    message_lower = message.lower()
    
    # ردود مبرمجة بسيطة
    if any(word in message_lower for word in ['hello', 'مرحبا', 'اهلا', 'السلام']):
        return "مرحباً! أنا مساعدك الطبي. كيف يمكنني مساعدتك اليوم؟"
    
    elif any(word in message_lower for word in ['طقس', 'weather']):
        return "أنا متخصص في المجال الطبي. للطقس يرجى استخدام تطبيقات الطقس المتخصصة."
    
    elif any(word in message_lower for word in ['دواء', 'علاج', 'medicine', 'drug']):
        return "أنا مساعد ذكي. للاستشارات الطبية يرجى استشارة طبيب متخصص."
    
    elif any(word in message_lower for word in ['شكر', 'thanks', 'thank']):
        return "عفوًا! سعيد بمساعدتك. هل لديك أي استفسارات أخرى؟"
    
    else:
        return "شكراً لرسالتك. أنا حالياً في مرحلة التطوير وسأكون قريباً قادراً على مساعدتك في الاستفسارات الطبية."

def save_conversation(user_input, bot_response):
    """حفظ المحادثة في قاعدة البيانات"""
    try:
        conn = sqlite3.connect('chatbot.db')
        c = conn.cursor()
        c.execute('INSERT INTO conversations (user_input, bot_response) VALUES (?, ?)', 
                 (user_input, bot_response))
        conn.commit()
        conn.close()
        logger.info("✅ تم حفظ المحادثة")
    except Exception as e:
        logger.error(f"❌ خطأ في حفظ المحادثة: {str(e)}")

if __name__ == '__main__':
    print("🚀 بدء تشغيل Medical Chatbot...")
    init_db()
    app.run(debug=True, host='0.0.0.0', port=5000)
📋 خطوات التشغيل السريع
انسخ الكود أعلاه واحفظه في ملف app_fixed.py

شغل التطبيق:

bash
python app_fixed.py
افتح المتصفح واذهب إلى:

text
http://localhost:5000
🔍 فحص الأخطاء الشائعة
تحقق من:
bash
# 1. المنافذ المستخدمة
netstat -an | findstr :5000  # Windows
lsof -i :5000                # Linux/Mac

# 2. أذونات الملفات
python -c "import os; print('القراءة:', os.access('app.py', os.R_OK)); print('الكتابة:', os.access('.', os.W_OK))"

# 3. الذاكرة المتاحة
python -c "import psutil; print(f'الذاكرة: {psutil.virtual_memory().percent}%')"
🎯 إ